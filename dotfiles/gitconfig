[user]
    name = Adam Suczewski
    email = adamlouis@users.noreply.github.com
    signingkey = 795A24A8F9D5F8DE

[commit]
    gpgsign = true

# includes configuration for git-delta: https://github.com/dandavison/delta
[core]
    pager = delta

[interactive]
    diffFilter = delta --color-only

[add.interactive]
    useBuiltin = false # required for git 2.37.0

[delta]
    navigate = true    # use n and N to move between diff sections
    light = false      # set to true if you're in a terminal w/ a light background color (e.g. the default macOS terminal)

[merge]
    conflictstyle = diff3

[diff]
    colorMoved = default